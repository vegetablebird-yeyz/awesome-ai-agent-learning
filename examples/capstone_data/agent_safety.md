# Agent 安全笔记

Agent 调用工具前，宿主程序应校验工具名称、参数类型和参数范围。不要把模型生成的文本直接交给 shell、`eval` 或数据库执行。

读取文件时应限定根目录、扩展名和文件大小。写入、删除、发送消息、支付等有外部影响的操作，应先向用户展示具体参数并等待人工确认。

模型输出和工具返回都属于不可信输入。系统需要设置超时、重试上限、最大步骤数和审计日志，避免无限循环并保留问题线索。
